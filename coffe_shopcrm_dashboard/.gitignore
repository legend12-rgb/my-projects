# secrets
.env
dashboard/.env.local

# python
coffeeshopvenv/
__pycache__/
*.pyc

# node (Phase 2)
dashboard/node_modules/
dashboard/.next/

# Demo-only (meeting) control — never commit this to GitHub
dashboard/src/components/DemoOrderButton.tsx

# Claude Code tooling / local MCP config — not part of the product
.claude/
.mcp.json
